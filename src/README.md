This is the summary of my workflow for this project:

# **STEP1:**
First as obvious as it is, I forked the main repo and then pulled it on my machine and started looking at what was in front of me.  
# **STEP2:**
Then I looked at the comments labelled as tasks in the file src/solver.py then i started completing the tasks one by one.  
# **STEP3:**
For task one it took some time for me to figure out using nested `for` loops to iterate between all the neighbours then i used dr and dc as the changing values, added them to the position of the specific cell in the grid. I then used the counter named `alive_count` that counts the number of alive neighbours. Also, I took care of the fact that the specific cell was not being counted using a small block of `if` statements.  
# **STEP4:**
Task 2 was heavier than task 1 and it took a more time to finish, I also suffered from fever for 2 days which halted my work, nevertheless i made it through by first making a new grid consisting of dead cells then using nested `for` loops for iterating through all cells in the grid, I then used my `count_neighbors` function to find out number of neighbours and then using this information I set up conditions based on the 'Rules Of Life' using `if` `elif` statements.  
# **STEP5:**
Inserted some inline comments for explaining the code.
# **STEP6:**
Finally I wrote this readme file explaining what i did in a minute and concise way so that it does not waste much of your time.


P.S. I have inserted inline comments in the code to help you understand better.

